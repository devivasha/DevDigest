{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"systemMessage\": \"Session ending — run /engineering-insights to capture any non-obvious findings, gotchas, or working patterns discovered this session. If nothing substantial came up, skip it. See CLAUDE.md session protocol.\"}'",
            "statusMessage": "Reminding Claude to run /engineering-insights..."
          }
        ]
      }
    ],
    "PreToolUse": [
       {
        "matcher": "Bash(git push*)",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Before executing this git push command, invoke the pr-self-review skill. Run: git diff $(git merge-base origin/main HEAD)...HEAD --name-only --diff-filter=AM to get changed files. Filter out vendor/, node_modules/, migrations/, *.md, *.json, e2e/. Route remaining files to buckets: UI Frontend (client/**/*.ts,tsx,css), Backend/Domain (server/**, reviewer-core/**), Tests (*.test.ts,tsx). Spawn one sub-agent per non-empty bucket, applying the skills assigned to each bucket per the pr-self-review skill. Also run: diff -r client/src/vendor/shared/contracts/ server/src/vendor/shared/contracts/ and add a CRITICAL finding if any differences exist. Collect all findings, deduplicate by file:line, sort by severity. Output the PR Self-Review Report in the format defined in the pr-self-review skill. If any CRITICAL findings exist: print 🚫 BLOCKER and DO NOT execute the git push — tell the user to fix CRITICAL issues first. If no CRITICAL findings: print ✅ PASS and proceed with git push."
          }
        ]
      }
    ]
  },
   "env": {
    "ENABLE_TOOL_SEARCH": "true"
  },
  "permissions": {
    "allow": [
      "Bash(git diff*)",
      "Bash(git log*)",
      "Bash(git status*)",
      "Bash(git add*)",
      "Bash(git commit*)",
      "Bash(git checkout*)",
      "Bash(git branch*)",
      "Bash(git fetch*)",
      "Bash(git pull*)",
      "Bash(git push*)",
      "Bash(pnpm*)",
      "Bash(npm*)",
      "Bash(node*)",
      "Bash(tsx*)",
      "Bash(docker ps*)",
      "Bash(docker exec*)",
      "Bash(curl*)",
      "Bash(cat*)",
      "Bash(ls*)",
      "Bash(find*)",
      "Bash(grep*)",
      "Bash(echo*)",
      "Read(*)",
      "Write(src/**)",
      "Write(.claude/**)"
    ],
    "deny": [
      "Bash(rm -rf*)",
      "Bash(sudo*)",
      "Bash(chmod 777*)",
      "Bash(git push --force*)",
      "Bash(docker rm*)",
      "Bash(dropdb*)",
      "Bash(git reset --hard*)"
    ]
  }
}
