# Insights — client

Non-obvious findings and gotchas. Add an entry whenever something surprised you,
so the next agent/session doesn't relearn it. Append-only — see the
`engineering-insights` skill for how entries are captured.

## What Works

- **2026-07-15** — T11 (Multi-Agent Tabs+detail): joined `AgentColumn.findings` (`AgentColumnFinding` — a subset shape with `id`/`severity`/`title`/`file`/`start_line`, no `confidence`/`suggestion`) back to the full persisted `FindingRecord` (has `confidence`, `suggestion`, `rationale`, `accepted_at`/`dismissed_at`) by building a `Map<id, FindingRecord>` from `usePrReviews(prId)`'s `ReviewRecord[]` and looking up the selected finding's id in it. `AgentColumnFinding` has no `end_line` at all (only `start_line`) — the shared `lineLabel` helper had to accept an optional `end_line`. ref: `client/src/app/multi-agent/[runId]/_components/MultiAgentTabs/helpers.ts`, `client/src/vendor/shared/contracts/observability.ts` (`AgentColumnFinding`)

- **2026-06-14** — `formatCost` (`src/lib/cost.ts`) distinguishes MISSING data (`null`/`undefined` → "—") from a genuine zero (`0` → "$0.00"), widens precision for sub-cent values (~2 sig figs), and trims trailing zeros to a 2dp floor ("$0.06" not "$0.060", "$0.0013" not "$0.00"). Reuse it for any per-run money display.

- **2026-07-15** — AgentDisagreement restyle (T12 follow-up): the design mockup's per-take verdict colors (CRITICAL → `var(--crit)`, WARNING → `var(--warn)`, SUGGESTION → `var(--accent)`) do NOT match `@devdigest/ui`'s `SeverityBadge`/`SEV` token map (`primitives/tokens.ts` uses `var(--sugg)` for SUGGESTION, not `var(--accent)`), so reusing `SeverityBadge` verbatim would silently diverge from the mockup on the suggestion color. Built a small local `verdictDotColor()` helper instead (a colored dot + the raw `ConflictTake.verdict` enum string as the label, e.g. literal "CRITICAL") rather than importing `SeverityBadge` — this also sidesteps needing the icon registry for per-take verdicts, since the enum text itself already differs per severity (satisfies "icon+text not colour-only" without needing an actual icon glyph on every take). ref: `client/src/app/multi-agent/[runId]/_components/AgentDisagreement/helpers.ts` (`verdictDotColor`), `client/src/vendor/ui/primitives/tokens.ts:10-13`

- **2026-07-15** — T14 (Results page view-mode toggle): the existing `@devdigest/ui` `Tabs` kit component (plain `<button>`s, no `role="tablist"`/`"tab"` ARIA) is naturally keyboard-operable and was reused verbatim for the Columns↔Tabs view-mode switcher, not just for per-agent tabs inside `MultiAgentTabs` — passing `tabs=[{key:"columns",label:...},{key:"tabs",label:...}]` and wrapping it in a `<div role="group" aria-label={t("viewMode.toggleAriaLabel")}>` gives an accessible, keyboard-reachable 2-option toggle for free with zero new UI kit code. ref: `client/src/app/multi-agent/[runId]/_components/ResultsView.tsx`, `client/src/vendor/ui/kit/Tabs.tsx`

- **2026-07-15** — T14 restyle (Results page header → design mockup): swapping the underline-style `Tabs` kit component out for a compact segmented control did NOT need the `role="group"`/`aria-label` wrapper to change — the AC-23 test only queries `getByRole("button", { name: "Tabs" })`, which is agnostic to the toggle's internal markup as long as the button's accessible name is exactly the label text (no icon inside the button, so no icon alt-text pollutes the computed name). Built the segmented control as a small module-scope `ViewModeToggle` function component (props: `mode`/`onChange`/label strings) rather than reusing `Tabs` with different `pad`/styling — `Tabs`' bottom-border "underline" look has no styling knob for a pill/segmented look, so a from-scratch 2-button `<div role="group">` was less code than fighting the shared primitive's inline styles. `@devdigest/ui`'s `Button` component (`kind="ghost" size="sm" icon="Settings"`) covers the "Configure run" nav button with zero custom styling — `Icon` name strings resolve through the same registry noted in prior sessions (`Cpu`/`Settings` both exist). ref: `client/src/app/multi-agent/[runId]/_components/ResultsView.tsx`, `client/src/vendor/ui/primitives/Button.tsx`

## What Doesn't Work

- **2026-06-20** — `position: absolute` popovers rendered inside the PR-list table are silently clipped: `tableCard` has `overflow: hidden` for its border-radius. Fix: use `position: fixed` + capture `getBoundingClientRect()` on `mouseEnter` and pass `{ top, left }` to the popover component. Evidence: `client/src/app/repos/[repoId]/pulls/styles.ts` (`tableCard`), `client/src/components/FindingsPopover.tsx`.
- **2026-06-20** — React hooks (`useState`, `useRef`) cannot be called inside a `.map()` render callback — React will throw "Rendered more hooks than during the previous render". If a list item needs its own hover or ref state, extract it to a named component (e.g. `RunRow` from `RunHistory`, `PRRow`). Evidence: `client/src/app/repos/[repoId]/pulls/[number]/_components/RunHistory/RunHistory.tsx`.
- **2026-07-15** — Conditional inline-style objects that spread a shorthand (`border: "1px solid var(--x)"`) as the base and then only override one longhand (`borderColor: "var(--y)"`) on the active variant trigger a React dev warning ("Removing a style property during rerender... don't mix shorthand and non-shorthand") on toggle/rerender, even though the test still passes. Fix: override the same shorthand property in the active variant too (`border: "1px solid var(--y)"`), not just the longhand. Evidence: `client/src/app/multi-agent/[runId]/_components/AgentDisagreement/styles.ts` (`toggle`/`toggleActive`).

## Codebase Patterns

- **2026-06-20** — `vendor/shared/` contracts (`@devdigest/shared`) are mirrored in BOTH `client/src/vendor/shared/` and `server/src/vendor/shared/` — NOT auto-synced. Any schema change (e.g. adding a field to `PrMeta` or `RunSummary`) must be applied to both copies in the same edit. Evidence: `client/src/vendor/shared/contracts/platform.ts`, `trace.ts`.
- **2026-06-14** — Cross-route shared components live in `src/components/<Name>/` with an `index.ts` barrel, imported via `@/components/<Name>` (e.g. `RunCostBadge`, `diff-viewer`). Vendored UI primitives (`Badge`, `CircularScore`) live in `src/vendor/ui` under `@devdigest/ui` — different home. Evidence: `client/src/components/RunCostBadge/`.
- **2026-06-14** — The PR-list table is driven by two parallel constants that MUST stay length-aligned: `COLUMN_KEYS` (header keys + order) and `GRID` (CSS grid-template tracks). Adding a column = add to both AND render a matching cell in `PRRow.tsx`, else header/cells misalign silently. Evidence: `client/src/app/repos/[repoId]/pulls/constants.ts`.
- **2026-06-14** — i18n has only the `en` locale (`client/messages/en/`); new UI strings need a key under the right namespace file (e.g. `prReview.json`, `runs.json`) read via `useTranslations("<ns>")`. A missing key renders the raw key, not an error.

- **2026-07-15** — `AgentDisagreement.tsx`'s per-group `ConflictRow`/`ConflictTakeBadge` has NO role/label/data-testid on the row or takes containers (plain `<div>`/`<span>`s), so scoping a test query to "this group only" needs either a `within(...)` over a structurally-found ancestor (`closest("div")!.parentElement` — brittle, breaks silently if a wrapper `<div>` is added/removed) or a fixture change. When the component is frozen (owned by another task) and personas/verdict text can be made unique per test fixture without losing coverage, prefer renaming test fixture personas (e.g. `disagreementGroup` keeps "Security Reviewer"/"Style Reviewer", `agreementGroup` uses distinct "Docs Reviewer"/"Perf Reviewer") so every assertion can use a plain unscoped `screen.getByText(...)` — this fully avoids DOM-structure coupling instead of just shortening it. Also: the icon next to "did not flag" is decorative (`<Icon.EyeOff/>` beside the text) — assert the visible text, not `closest("span")?.querySelector("svg")`. ref: `client/src/app/multi-agent/[runId]/_components/AgentDisagreement/AgentDisagreement.tsx` (`ConflictRow`, `ConflictTakeBadge`), `client/src/app/multi-agent/[runId]/_components/AgentDisagreement/AgentDisagreement.test.tsx`

- **2026-07-06** — When adding a new shared contract file (e.g. `contracts/blast.ts`), the contract file itself must be byte-identical between `server/src/vendor/shared/` and `client/src/vendor/shared/`, but the barrel `index.ts` re-export line is NOT byte-identical across the two — server's barrel uses `.js` extensions (`export * from './contracts/blast.js'`), client's omits them (`export * from './contracts/blast'`). Match each barrel's existing convention rather than copying the barrel line verbatim. ref: `client/src/vendor/shared/index.ts:19`, `server/src/vendor/shared/index.ts:19`

- **2026-07-10** — `client/insights/gotchas.md` still says `@devdigest/shared` "resolves to `../server/src/vendor/shared`" — that's stale. `client/tsconfig.json` actually points `@devdigest/shared` at `./src/vendor/shared/index.ts` (the client's OWN mirrored copy), matching the "mirrored in BOTH, not auto-synced" pattern above. Trust the tsconfig `paths` entry over that gotchas.md line if they ever conflict. ref: `client/tsconfig.json:24`, `client/insights/gotchas.md`

- **2026-07-10** — Client's `Skill` schema has drifted from server's independent of any T1/T1b change: client has `version: z.number().int()` and `evidence_files: z.array(z.string()).nullish()`; server has `version: z.number()` (no `.int()`) and `evidence_files: z.array(z.string()).nullable()` (not `.nullish()`). Pre-existing drift, not introduced by this session — flagging for a future cleanup pass rather than fixing silently mid-task. ref: `client/src/vendor/shared/contracts/knowledge.ts:146-147`, `server/src/vendor/shared/contracts/knowledge.ts:146-147`

## Tool & Library Notes

- **2026-07-13** — `api.get<T>`/`api.post<T>` (`client/src/lib/api.ts`) perform an UNCHECKED cast to `T` — no runtime schema validation. So drift between a route's declared Fastify `response` schema (server) and the client hook's generic compiles cleanly on BOTH sides' `pnpm typecheck` and surfaces only at runtime or in code review. Concrete L06 case caught by the architecture review, NOT by tsc: `POST /agents/:id/eval-runs` returns a flat `EvalRun`, but `useRunEvalSet`/`useRunAllAgents` typed it `EvalSetRunResult` (`{set_run_id, result: EvalRun}`) and `EvalsTab.handleRunAll` read `data.result.per_trace` → would throw `Cannot read properties of undefined` on every "Run all evals" click. Fix: align the client to what the route actually returns (`EvalRun`/`data.per_trace`). When wiring a hook to a route, cross-check the route's `response` schema — the generic on `api.*` is a promise you make to yourself, not a guarantee tsc enforces across the client/server boundary. ref: `client/src/lib/api.ts`, `client/src/lib/hooks/eval.ts` (`useRunEvalSet`)

- **2026-07-06** — `@testing-library/user-event` is NOT installed in `client/` (import fails with "Failed to resolve import" in vitest) — use `fireEvent` from `@testing-library/react` instead, matching the existing pattern in `FindingCard.test.tsx`. ref: `client/src/app/repos/[repoId]/pulls/[number]/_components/OverviewTab/BlastRadiusCard.test.tsx`

- **2026-07-11** — `client/insights/gotchas.md`'s "`fetch` is mocked globally in tests" claim is STALE: `src/test/setup.ts` only installs `@testing-library/jest-dom/vitest` matchers and a `ResizeObserver` stub — there is no global `fetch` mock anywhere in the client. No existing test (as of T14) relies on a global fetch mock either; every prior test that needs server data mocks `@/lib/hooks` directly instead (see `ProjectContextView.test.tsx`, `AgentCard.test.tsx`). `OnboardingTourView.test.tsx` (T14) needed the real fetch boundary (per its task brief), so it stubs `fetch` itself per-test via `vi.stubGlobal("fetch", fetchMock)` in `beforeEach` / `vi.unstubAllGlobals()` in `afterEach` — trust this over the gotchas.md line until that file is corrected. ref: `client/src/test/setup.ts`, `client/insights/gotchas.md`

- **2026-07-11** — `src/test/setup.ts` stubs `ResizeObserver` but NOT `IntersectionObserver`. Any component using `IntersectionObserver` directly (e.g. `OnboardingTourView`'s `useActiveSectionId` for the "on this page" nav highlight) throws `ReferenceError: IntersectionObserver is not defined` under jsdom the moment it mounts, as an uncaught passive-effect exception (not a normal test failure — shows up under "Unhandled Errors" and can silently mark unrelated tests as false positives). Fix at the test-file level (test files may add global stubs; production `setup.ts` is out of test-writer's edit scope): mirror the existing `ResizeObserver` stub pattern with `if (typeof globalThis.IntersectionObserver === "undefined") { globalThis.IntersectionObserver = class { observe(){} unobserve(){} disconnect(){} } as unknown as typeof IntersectionObserver; }` at module scope before rendering. ref: `client/src/app/repos/[repoId]/onboarding/_components/OnboardingTourView/OnboardingTourView.tsx:42`, `client/src/app/repos/[repoId]/onboarding/_components/OnboardingTourView/OnboardingTourView.test.tsx`

- **2026-07-11** — `DegradedBanner`'s reason text renders as a single text node `" — " + t(...)` inside one `<span>` (`<span> — {t(...)}</span>`), so RTL's `getByText("exact reason string")` fails ("text is broken up") even though the reason text is visibly present — the em-dash prefix is part of the same text node. Use a regex substring matcher (`screen.getByText(/exact reason string\./)`) instead of an exact string when asserting on the degraded-reason line. ref: `client/src/app/repos/[repoId]/onboarding/_components/OnboardingTourView/_components/DegradedBanner.tsx:21`

- **2026-07-11** — `SectionCard` wraps each of the 5 Onboarding Tour sections in `<section id={id} aria-label={title}>` — this gets the implicit ARIA `region` role (a `<section>` only maps to `role="region"` when it has an accessible name), so `screen.getByRole("region", { name: "<section title>" })` is a clean, scoped way to assert/query within one section without adding a `data-testid`. ref: `client/src/app/repos/[repoId]/onboarding/_components/OnboardingTourView/_components/SectionCard.tsx:21`

- **2026-07-11** — For `MermaidDiagram`'s dynamic `await import("mermaid")`, a top-level `vi.mock("mermaid", () => ({ default: { initialize, parse, render } }))` intercepts the dynamic import fine in Vitest (no special handling needed for `import()` vs static `import`). Make the mocked `render` resolve `{ svg: '<svg role="img" aria-label="...">...</svg>' }` — since `MermaidDiagram` does `ref.current.innerHTML = svg` verbatim, embedding an accessible `role="img"` + `aria-label` in the fake SVG string lets the test assert the diagram container rendered via `getByRole("img", {name})` instead of raw DOM/snapshot inspection. Use `vi.hoisted()` to share the mock fns between the `vi.mock` factory (hoisted above imports) and per-test assertions/`vi.clearAllMocks()`. ref: `client/src/components/mermaid-diagram/MermaidDiagram.tsx:45`, `client/src/app/repos/[repoId]/onboarding/_components/OnboardingTourView/OnboardingTourView.test.tsx`

- **2026-07-11** — `"AlertCircle"` is NOT a valid icon name in `@devdigest/ui`'s icon registry (same class of gap as `"Package"`/`"Network"`) — the registry only has `AlertTriangle`/`AlertOctagon`/`Info` for the alert family. Used `AlertOctagon` for a `medium`-severity risk-level badge instead. ref: `client/src/vendor/ui/icons.tsx:24-26`, `client/src/app/repos/[repoId]/pulls/[number]/_components/OverviewTab/PrBriefCard.tsx`

- **2026-07-11** — `Badge` (`client/src/vendor/ui/primitives/Badge.tsx`) renders its icon `<svg>` as a sibling of the text `children`, both direct children of the outer `<span>` — the icon SVG contributes no `textContent`, so `screen.getByText("Medium risk")` resolves to that outer `<span>` itself (not a nested text node), letting you assert "icon + label both present" with `badge.querySelector("svg")` scoped off the same element `getByText` returned, no `data-testid` needed. Same holds for `Skeleton` (`client/src/vendor/ui/primitives/Skeleton.tsx`) — it's a bare `<div className="skeleton">` with no accessible role/name at all, so a scoped `container.querySelectorAll(".skeleton")` is the only way to assert a loading state renders; there is no RTL role/text query alternative for either case. ref: `client/src/vendor/ui/primitives/Badge.tsx:22-46`, `client/src/vendor/ui/primitives/Skeleton.tsx`, `client/src/app/repos/[repoId]/pulls/[number]/_components/OverviewTab/PrBriefCard.test.tsx`

- **2026-07-11** — When a component's data hooks are fully module-mocked (`vi.mock("@/lib/hooks/...")` returning plain `vi.fn()`s), changing a mock's `mockReturnValue` between simulated async phases (e.g. `isPending: false → true → false` for a mutation) does NOT by itself re-render the component — the mock isn't real React state, so React has no signal to re-render. Must explicitly call RTL's `rerender(...)` with the same wrapped element after each `mockReturnValue` change to see the new phase reflected in the DOM. This is a reasonable way to test a component's reaction to hook states without re-implementing TanStack Query's real async machinery. ref: `client/src/app/repos/[repoId]/pulls/[number]/_components/OverviewTab/PrBriefCard.test.tsx`

- **2026-07-12** — T17 (eval-pipeline client tests): `EvalDashboardView`/`AgentEvalDetail` (T13) render the SAME agent name as text in two places at once once the recent-runs table has a row: once inside the clickable `AgentSummaryCard` `<button>`, once as a plain `<td>` cell in the "Recent eval runs" table. A bare `screen.getByText("Security Reviewer")` throws "multiple elements found" — must either use `getAllByText(...)` (assert `.length > 0`) or scope with `within(screen.getByRole("table"))` / `.closest("button")` to disambiguate which instance is meant. Same applies to the `v{version}` string, which appears both in the card's subtitle and the table's `v` column. ref: `client/src/app/eval/_components/EvalDashboardView.tsx`, `client/src/app/eval/_components/EvalDashboardView.test.tsx`

- **2026-07-12** — T17: `EvalCaseEditorModal`'s `FormField` label (`@devdigest/ui`'s `client/src/vendor/ui/kit/FormField.tsx`) renders a bare `<label>` with no `htmlFor`/`id` wiring to its input — so `getByLabelText("Name")` / `getByLabelText("Expected output")` do NOT work for this modal's fields (RTL can't associate the label with the control). The Name field must be queried by its placeholder (`getByPlaceholderText("stripe-key-leak")`); the Expected-output `Textarea` has no placeholder at all and is always the LAST `role="textbox"` element in DOM order on the default "diff" tab (order: Name input → Diff textarea → Expected-output textarea) — `screen.getAllByRole("textbox")` and take the last element. ref: `client/src/vendor/ui/kit/FormField.tsx`, `client/src/components/eval/EvalCaseEditorModal.tsx`, `client/src/components/eval/EvalCaseEditorModal.test.tsx`

- **2026-07-15** — T10 (Multi-Agent Columns): plain `nvm use 22` in a Bash tool call silently no-ops if you don't `export NVM_DIR="$HOME/.nvm"; source "$NVM_DIR/nvm.sh"` in the SAME command first — each Bash tool invocation is a fresh non-login shell, so nvm's own shell function isn't loaded by default and `node -v` still reports the system Node (v16) from `/usr/local/bin/node`, which then fails vitest with a cryptic `crypto$2.getRandomValues is not a function` startup error (a Node ≥20 Web Crypto API gap, not a real config bug). Fix: always chain `export NVM_DIR="$HOME/.nvm"; source "$NVM_DIR/nvm.sh"; nvm use 22; <command>` in one Bash call. ref: `client/package.json` (engines), Node v16.17.0 at `/usr/local/bin/node`

- **2026-07-15** — T9 (AgentPicker): `@testing-library/user-event` is NOT installed (`client/package.json` only lists `@testing-library/jest-dom` and `@testing-library/react`) — importing it in a new test file fails Vite import-analysis with "Failed to resolve import" and reports 0 tests collected (not a per-test failure, the whole file fails to load). Use `fireEvent` from `@testing-library/react` instead, matching the existing convention already used in `BlastRadiusCard.test.tsx`/`AgentEditor.test.tsx`. ref: `client/src/app/repos/[repoId]/pulls/[number]/_components/AgentPicker/AgentPicker.test.tsx`, `client/package.json`

- **2026-07-15** — Self-review fix (AgentPicker MEDIUM finding): the panel container had invalid ARIA nesting — `role="listbox"`/`aria-multiselectable="true"` wrapping `role="checkbox"` children (a listbox must contain `role="option"`, not checkboxes). Fixed as the smaller of the two valid patterns: dropped `listbox`/`aria-multiselectable`, made the container `role="group"` with an `aria-label`, kept children as `role="checkbox"` + `aria-checked` unchanged. For the group's accessible name, reused the ALREADY-EXISTING `configure.agentsLabel` ("Agents") key from the same `multiAgent` namespace via the same `t()` instance (`t("configure.agentsLabel")`) — since `useTranslations("multiAgent")` exposes the whole namespace tree, no new i18n key or second `useTranslations` call was needed even though `messages/en/multiAgent.json` was out of this task's owned paths. Also updated the trigger button's `aria-haspopup="listbox"` to `aria-haspopup="true"` to match — it was only ever correct because the popup used to literally be a listbox. ref: `client/src/app/repos/[repoId]/pulls/[number]/_components/AgentPicker/AgentPicker.tsx:75,85`, `client/messages/en/multiAgent.json` (`configure.agentsLabel`)

- **2026-07-15** — T9 (AgentPicker): the shared `Checkbox` (`client/src/vendor/ui/kit/Checkbox.tsx`) takes only `checked`/`onChange`/`label` — no `aria-label`/`aria-checked` passthrough, and it wraps the control in a bare `<label>` with no `htmlFor`, so a `role="checkbox"` button inside it gets no accessible name from that association. No existing consumer uses it yet (confirmed zero matches under `client/src/app`). Rather than edit the shared kit file (out of this task's owned paths), `AgentPicker` renders its own inline `role="checkbox"` buttons with an explicit `aria-label` (from the `multiAgent.picker.selectAgentAriaLabel` key) instead of importing the shared component. ref: `client/src/vendor/ui/kit/Checkbox.tsx`, `client/src/app/repos/[repoId]/pulls/[number]/_components/AgentPicker/AgentPicker.tsx`

## Recurring Errors & Fixes

- **2026-07-15** — Review-gate fix (AC-16 reload-safety): `MultiAgentColumns/helpers.ts`'s `resolveColumnStatus` fallback (no live SSE event) read `column.summary` for the failed reason, but `summary` only ever comes from a persisted `reviews` row — which a failed run never has (server-side `loadColumns` sets `summary: review?.summary ?? null`). Result: the failed reason was ALWAYS null after a page reload, even though `MultiAgentColumns.test.tsx`'s existing "AC-16" unit test passed — the test fixture manually set `summary: "Provider timed out"` on the mock `AgentColumn` object, which isn't how a real failed column's data ever looks (real ones have `summary: null`). A green test on a hand-built fixture that doesn't match the real DB-shape a failed run produces is NOT proof the reload path works — check what the upstream repository actually returns for the "failed" case before trusting a passing fixture-based test. Fixed by adding a real `AgentColumn.error` field (server surfaces `agent_runs.error`) and switching the fallback to read `column.error` instead. ref: `client/src/app/multi-agent/[runId]/_components/MultiAgentColumns/helpers.ts` (`resolveColumnStatus`), `client/src/app/multi-agent/[runId]/_components/MultiAgentColumns/MultiAgentColumns.test.tsx`

- **2026-07-15** — T11: a new App Router segment nested under a bracketed dynamic dir (e.g. `app/multi-agent/[runId]/_components/MultiAgentTabs/`) is 5 path segments below `src/` (`app`, `multi-agent`, `[runId]`, `_components`, `MultiAgentTabs`), so importing `src/lib/hooks/*` from a file directly inside that component folder needs exactly `../../../../../lib/hooks/...` (5 ups), not 6 — easy to miscount by one when copying the pattern from a deeper existing path like `FindingsPanel` (7 segments, 8 ups incl. messages). Vite's error ("Failed to resolve import") is a quick tell; fix by counting directory segments from `src/`, not by pattern-matching another file's up-count. ref: `client/src/app/multi-agent/[runId]/_components/MultiAgentTabs/MultiAgentTabs.tsx`

- **2026-07-13** — RESOLVED (integration pass): the "button no-ops in production" gap flagged in the 2026-07-12 T11 Codebase-Patterns entry — `ReviewRunAccordion.tsx` now passes `agentId={review.agent_id}` into `<FindingsPanel>`, so one-click "Turn into eval case" works end-to-end. `ReviewRecord.agent_id` is the owning agent; the client still sends only `{finding_id}` and the server re-derives ownership (`finding→review→agent`). ref: `client/src/app/repos/[repoId]/pulls/[number]/_components/ReviewRunAccordion/ReviewRunAccordion.tsx` (FindingsPanel render)

- **2026-07-13** — RESOLVED (integration pass): the missing `eval.json` keys flagged in the two 2026-07-12 T10/T12 Open-Questions entries were added (`caseEditor.cancel`/`runOnSave`/`findingSkeleton`/`tabs.files`, `dashboard.compare.close`, and `evalsTab.expectedGot`/`runAll`/`viewDashboard`/`tracesPassed`/`passingCount`/`emptyBrackets`/`loadError`). NON-OBVIOUS gotcha found while adding them: a MISSING key falls back to the key string harmlessly (existing gotcha), but a message that references an ICU variable the call site does NOT pass is a different, louder failure class — so placeholders had to be matched to the exact `t()` args by reading each call site first, not guessed: `evalsTab.expectedGot` → `{expected, got}`, `evalsTab.passingCount` → `{passing, total}`. ref: `client/messages/en/eval.json`, `client/src/app/agents/[id]/_components/AgentEditor/_components/EvalsTab/EvalsTab.tsx`

- **2026-07-13** — RESOLVED (final cleanup): the `FindingCard.test.tsx` `MISSING_MESSAGE: 'eval'` console noise flagged in the 2026-07-12 T17 Open-Questions entry is fixed by adding `eval: evalMessages` (import of `messages/en/eval.json`) to its `renderWithIntl` `NextIntlClientProvider` — same fix as `FindingsPanel.test.tsx`. The "Turn into eval case" button's real accessible name now resolves in that test instead of the raw key. ref: `client/src/app/repos/[repoId]/pulls/[number]/_components/FindingCard/FindingCard.test.tsx`

- **2026-06-29** — React duplicate-key warning from the diff viewer ("Encountered two children with the same key, `CLAUDE.md`") traced to duplicate rows in the `prFiles` DB table, not to a bug in the component. The key was the file path — unique in theory but not in the data. Fix: server deduplicates before returning, and `SmartDiffViewer` adds a client-side `Set<string>` guard for stale cache (60 s `staleTime`). ref: `client/src/components/diff-viewer/SmartDiffViewer/SmartDiffViewer.tsx:45`

- **2026-06-29** — `"Package"` is not a valid icon name in `@devdigest/ui`'s icon registry — TypeScript throws `Type '"Package"' is not assignable`. Use `"Boxes"` as the nearest substitute for a package/container icon. ref: `client/src/components/diff-viewer/SmartDiffViewer/GroupSection.tsx`

- **2026-07-06** — `"Network"` and `"ArrowUpRight"` are not valid icon names in `@devdigest/ui`'s icon registry either (same class of gap as `"Package"`) — the registry only re-exports a fixed lucide-react subset (`client/src/vendor/ui/icons.tsx`). Used `"Workflow"` for a graph-view toggle and `"CornerDownRight"` for a caller-arrow bullet as the nearest substitutes. Check `icons.tsx`'s export list before assuming a lucide icon name is available. ref: `client/src/vendor/ui/icons.tsx`, `client/src/app/repos/[repoId]/pulls/[number]/_components/OverviewTab/BlastRadiusCard.tsx`

- **2026-07-06** — Next build error "Module not found: Can't resolve './brief.js'" came from a new client shared-contract file (`contracts/blast.ts`) importing a SIBLING contract with a `.js` extension (`import { ChangedSymbol } from './brief.js'`). CORRECTS the earlier "contract files are byte-identical across client/server" note: intra-contract relative imports ALSO follow each side's extension convention — server uses `./brief.js` (NodeNext ESM), client uses `./brief` (webpack/Next can't resolve a `.js` specifier that points at a `.ts` source). So a contract file that imports a sibling is NOT byte-identical across the two vendors; only extension-free contracts are. Fix: drop `.js` on the client copy, matching `review-api.ts`'s `from './brief'`. ref: `client/src/vendor/shared/contracts/blast.ts:2`, `client/src/vendor/shared/contracts/review-api.ts:3`

- **2026-07-05** — "Cannot reach the DevDigest engine at http://localhost:3001. Is the API running?" is a CATCH-ALL for ANY failed `fetch` in `api.ts` — including a CORS rejection — NOT proof the API is down (the API can return 200 on `/health` while the browser call still fails). Root cause found this session: the client `dev` script defaulted to `next dev -p ${WEB_PORT:-3002}` while the server allows exactly ONE CORS origin `http://localhost:${WEB_PORT}` (=3000, `app.ts:90`). UI served from :3002 → cross-origin fetch to :3001 blocked → TypeError surfaced as the "engine unreachable" message. Fix: align the client default port to 3000. ref: `client/src/lib/api.ts:37`, `client/package.json`

- **2026-07-10** — Adding a new required field with `.default([])` to an existing exported Zod schema (e.g. `attached_doc_paths` on `Agent`/`Skill`, `specs_missing` on `RunTrace`) breaks `pnpm typecheck` — NOT `pnpm test` — in any test file that builds an object literal typed as the exported `z.infer` type without going through `.parse()`. Reason: zod v3's `z.infer` uses the OUTPUT type, where a `.default()` field is required (not optional), even though `.parse({})` would fill it in. `vitest` (esbuild, transpile-only) doesn't type-check so those same tests still pass `pnpm test`. Fix: add the new field with its default value directly to each literal fixture (`AgentCard.test.tsx`, `AgentEditor.test.tsx`, `RunTraceDrawer.test.tsx` needed this for T1b). Always run `pnpm typecheck` after adding a `.default()` field to a shared contract, not just `pnpm test`. ref: `client/src/vendor/shared/contracts/knowledge.ts`, `client/src/app/agents/_components/AgentCard/AgentCard.test.tsx:11`

- **2026-07-12** — RESOLVED (T17): the `FindingsPanel.test.tsx` "No QueryClient set" regression flagged in the 2026-07-12 T11 entry above is fixed by mocking `../../../../../../../lib/hooks/eval`'s `useCreateCaseFromFinding` the same way the file already mocks `lib/hooks/reviews`' `useFindingAction` — no `QueryClientProvider` wrapper needed once the hook itself is mocked at the boundary (mocking the hook, not just the underlying `api` module, sidesteps TanStack Query entirely). The test file's `renderWithIntl` helper also had to add `eval: evalMessages` to the `NextIntlClientProvider` messages object — `FindingCard`'s "Turn into eval case" button reads `useTranslations("eval")` for its label/aria-label, and without that namespace present next-intl's `MISSING_MESSAGE` fallback renders the raw key path as the accessible name, which breaks `getByRole("button", { name: "Turn this finding into an eval case" })`-style assertions. ref: `client/src/app/repos/[repoId]/pulls/[number]/_components/FindingsPanel/FindingsPanel.test.tsx`

## Session Notes

- **2026-07-13** — L06 Eval Pipeline (client surfaces), built via the spec→plan→implement→test→verify pipeline (multi-agent, disjoint owned paths). New: `lib/hooks/eval.ts`; `components/eval/**` (case-editor modal with live `EvalExpectation` JSON validation gating Save, compare-runs modal with a system-prompt diff, metric-trend chart, regression banner, delta badge, degraded/pass icons); `app/eval/**` (all-agents dashboard + single-agent detail with trend chart + selectable-runs Compare); an Evals tab in AgentEditor; the `/eval` nav item; and the FindingCard "Turn into eval case" button (disabled when untriaged, AC-4). Direction/regression conveyed by arrow+text, not colour (AC-23). Final: client 22 files / 70 tests green, typecheck clean. Files: `client/src/lib/hooks/eval.ts`, `client/src/components/eval/`, `client/src/app/eval/`, `client/src/app/agents/[id]/_components/AgentEditor/_components/EvalsTab/`.

- **2026-06-29** — Smart Diff feature (token-free): `SmartDiffViewer` groups changed files into core/wiring/boilerplate sections using pure path-pattern classification on the server; finding badges per file and line-level severity chips link to the Agent runs tab via `targetFindingId` state + `data-finding-id` + `scrollIntoView`. "What this does" section uses static patch analysis (no LLM). Files: `SmartDiffViewer/`, `CodeLine/CodeLine.tsx`, `DiffTab/DiffTab.tsx`, `page.tsx`, `ReviewRunAccordion/ReviewRunAccordion.tsx`.
- **2026-06-29** — Conventions "suggested skills": `useSkills()` + filter `type === 'convention'` gives the skills already formalized from prior scans; passed as `suggestedSkills` prop to `ConventionCard` and rendered as accent chips. Files: `ConventionsView/ConventionsView.tsx`, `ConventionCard/ConventionCard.tsx`.

- **2026-07-06** — Blast Radius card polish: placed Intent + Blast cards side-by-side in `OverviewTab.tsx` (a `flex` row of two `flex: 1 1 360px` columns, `flexWrap: wrap` so they stack on narrow viewports, `alignItems: flex-start` so unequal heights don't stretch). Filtered caller-less symbols out of the tree so only symbols with ≥1 caller render (`symbolsWithCallers`). Interim experiment to hide zero-count segments in the top summary + show a "downstream not determined" badge was REVERTED per user — the summary strip keeps full counts incl. zeros. Files: `OverviewTab/OverviewTab.tsx`, `OverviewTab/BlastRadiusCard.tsx`, `messages/en/blast.json`.

## Codebase Patterns

- **2026-06-29** — Cross-tab click-to-navigate (diff → findings) pattern: `page.tsx` holds `targetFindingId` state; `handleFindingClick` sets it and switches the tab; `FindingsTab` passes it to `ReviewRunAccordion`; the accordion `useEffect` opens itself if it owns the target finding and calls `querySelector('[data-finding-id="…"]').scrollIntoView()` after an 80 ms delay (accordion animation). ref: `client/src/app/repos/[repoId]/pulls/[number]/page.tsx`, `_components/ReviewRunAccordion/ReviewRunAccordion.tsx`

- **2026-06-29** — Convention skills have `type === 'convention'` (set by `createSkillFromAccepted` on the server). Filter `useSkills()` result by this type to find skills that originated from the Conventions workflow — used to show "Suggested skills" chips on `ConventionCard`. ref: `client/src/app/conventions/_components/ConventionsView/ConventionsView.tsx:29`

- **2026-07-06** — Mocking a hook via its `@/` tsconfig-path alias (e.g. `vi.mock("@/lib/hooks/pulls", () => ({ useBlastRadius: ... }))`) resolves correctly in vitest and matches the same module the component under test imports — no need to compute a deep relative path (`../../../../../lib/hooks/pulls`) like older tests in this codebase do. Simplifies mocking hooks for components several directories deep. ref: `client/src/app/repos/[repoId]/pulls/[number]/_components/OverviewTab/BlastRadiusCard.test.tsx`

- **2026-07-10** — T15 (specs_missing UI row): when an implementer task's Owned paths list only `TraceBody.tsx` (not the shared `RunTraceDrawer/styles.ts`), a new one-off visual style (e.g. the specs-missing strikethrough/warn styling) should be defined as a local module-level `CSSProperties` const inside the owned file rather than added to `styles.ts` — keeps the change inside the task's file scope and avoids touching another task's territory in a parallel run. ref: `client/src/app/repos/[repoId]/pulls/[number]/_components/RunTraceDrawer/_components/TraceBody/TraceBody.tsx:23`

- **2026-07-11** — T11: no existing test references `activeKeyFor` or `NAV` (`grep -rn "activeKeyFor" --include="*.test.*" src/` → empty), so tightening the `/onboarding` branch to require `pathname.includes("/repos") && pathname.includes("/onboarding")` had zero test fallout — full suite (15 files / 43 tests) plus `pnpm typecheck` stayed green with no other changes needed. ref: `client/src/components/app-shell/helpers.ts:29-30`

- **2026-07-12** — T11 (Turn into eval case): wiring `FindingsPanel` to call `useCreateCaseFromFinding()` unconditionally (Rules of Hooks — can't call it only when an `onCreateEvalCase` handler is needed) breaks the existing `FindingsPanel.test.tsx` smoke tests with `"No QueryClient set, use QueryClientProvider to set one"` — that test file mocks `lib/hooks/reviews` but not `lib/hooks/eval`, and renders without a `QueryClientProvider` (unlike `AgentCard.test.tsx`, which does both). This is expected per the plan DAG (`T11 --> T17`): T17 owns updating this test to `vi.mock("../../../../../../../lib/hooks/eval", ...)` and wrap in a `QueryClientProvider`, same pattern as `AgentCard.test.tsx`. Not fixed in T11 since `.test.tsx` files were out of T11's owned paths. ref: `client/src/app/repos/[repoId]/pulls/[number]/_components/FindingsPanel/FindingsPanel.test.tsx`, `client/src/app/agents/_components/AgentCard/AgentCard.test.tsx`

- **2026-07-12** — T11: `FindingsPanel` has no existing prop carrying the review's owning agent id (`ReviewRecord.agent_id`) — only `findings: FindingRecord[]` is passed down from `ReviewRunAccordion.tsx`, and `FindingRecord` itself has no `agentId` field (only `review_id`). Added an optional `agentId?: string | null` prop to `FindingsPanel` for "Turn into eval case" to route through (`useCreateCaseFromFinding({agentId, findingId})`), but `ReviewRunAccordion.tsx` was NOT in T11's owned paths so it still does not pass `agentId={review.agent_id}` down — the button renders correctly (enabled/disabled per triage state per AC-4) but clicking it in production today no-ops silently (`onCreateEvalCase` is `undefined` when `agentId` is falsy) until a follow-up task wires `<FindingsPanel agentId={review.agent_id} .../>` in `ReviewRunAccordion.tsx`. ref: `client/src/app/repos/[repoId]/pulls/[number]/_components/FindingsPanel/FindingsPanel.tsx`, `client/src/app/repos/[repoId]/pulls/[number]/_components/ReviewRunAccordion/ReviewRunAccordion.tsx:180`

- **2026-07-12** — T13 (Eval Dashboard `/eval`): `activeKeyFor()` (`client/src/components/app-shell/helpers.ts:37`) already had `if (pathname.startsWith("/eval")) return "eval";` wired in before T13 landed — no app-shell edit was needed to make the new nav item highlight correctly; only `vendor/ui/nav.ts` needed the new `NavItemDef`. ref: `client/src/components/app-shell/helpers.ts:37`, `client/src/vendor/ui/nav.ts`

- **2026-07-12** — T13: `NavItem` (`client/src/vendor/ui/shell/NavItem.tsx:54`) renders `item.label` verbatim from `nav.ts` — it is NOT run through `useTranslations`. `nav.eval` in `shell.json` is only consumed by the command palette (`useShellCommands.ts:24`, `t(\`nav.${it.key}\`)`), not by the sidebar itself. So the literal English `label: 'Eval Dashboard'` in `nav.ts` (as the plan's T13 action specified verbatim) is consistent with every other `NavItemDef` in the file — none of them are translated in the sidebar today. ref: `client/src/vendor/ui/shell/NavItem.tsx:54`, `client/src/vendor/ui/shell/Sidebar.tsx`, `client/src/components/app-shell/hooks/useShellCommands.ts:24`

- **2026-07-12** — T13: no dedicated "per-agent eval summary" endpoint exists (`GET /eval/dashboard` returns one workspace-wide `EvalDashboard` with a single `recent_runs: EvalSetRunRecord[]` list, not per-agent breakdowns). Built the `/eval` all-agents cards (AC-20: name, model, last run vN, pass count, recall/precision/citation, trend) by grouping that flat `recent_runs` list by `owner_id` client-side (`buildAgentSummaries` in `EvalDashboardView.tsx`) rather than adding a new hook — every `EvalSetRunRecord` already carries `owner_id`/`owner_kind`/`version`/metrics, so no new server surface was needed. ref: `client/src/app/eval/_components/EvalDashboardView.tsx`, `client/src/vendor/shared/contracts/eval-ci.ts:82-99`

- **2026-07-12** — T13: `client/messages/en/eval.json` is read-only for this task (integration owns net-new keys), but every string needed for `/eval` + the single-agent detail was already coverable by EXISTING keys — no missing-key fallback needed for the eval namespace. For one label with no existing eval.json key (the "Agent" column header on the all-agents recent-runs table), reused `shell.json`'s `nav.agents` ("Agents") via a second `useTranslations("shell")` call in the same component instead of inventing a new `eval.json` key or hardcoding English — a clean way to avoid both problems when a suitable string already exists in a different namespace. ref: `client/src/app/eval/_components/EvalDashboardView.tsx`, `client/messages/en/shell.json:22`

- **2026-07-12** — T13: `"GitCompare"` is NOT a valid icon name in `@devdigest/ui`'s icon registry (same class of gap as `"Package"`/`"Network"`/`"AlertCircle"` noted in prior sessions) — used `"GitMerge"` as the nearest substitute for the Compare-runs button. ref: `client/src/vendor/ui/icons.tsx`, `client/src/app/eval/_components/AgentEvalDetail.tsx`

- **2026-07-13** — RESOLVED (closes the 2026-07-12 T12 gap logged above — "no single-case run endpoint, per-case status only from in-session `lastTraces`"): added `useCaseStatuses(agentId)` (`GET /agents/:id/eval-cases/status`, key `['eval-case-status', agentId]`) and `useRunEvalCase(agentId)` (`POST /agents/:id/eval-cases/:caseId/run`) to `client/src/lib/hooks/eval.ts`. `EvalsTab.tsx`'s row status now follows an explicit precedence: `sessionTrace = lastTraces[c.name]` (this-session run, from either `handleRunAll` or a per-row `runCase.mutate`) wins when present; otherwise falls back to `statusByCaseId.get(c.id)` (the server-persisted latest status, loaded on mount via `useCaseStatuses`) — a case run in a PREVIOUS session/tab now correctly shows its real pass/fail icon on load instead of always "never run". The per-row play button was rewired from `handleRunAll` (whole-set) to `runCase.mutate({ caseId })` (that row only); the case-editor modal's `onRunCase` was similarly rewired to run only the case being edited. `useRunEvalSet`'s `onSuccess` now also invalidates `['eval-case-status', agentId]` so a full-set run refreshes the row icons from the server, not just from the in-session trace. ref: `client/src/lib/hooks/eval.ts`, `client/src/app/agents/[id]/_components/AgentEditor/_components/EvalsTab/EvalsTab.tsx`.

- **2026-07-13** — T18 (Eval Dashboard polish — sub-header, agent/date-range pickers, Compare modal legend): `@devdigest/ui`'s `kit/Dropdown.tsx` (barrel-exported, already used by `shell/RepoSwitcher.tsx`) is the codebase's general-purpose select/menu primitive — trigger is any `ReactNode`, items are `DropdownItemDef[]` with `label`/`icon`/`onClick`. Reused it as-is for both the agent-picker (`AgentPickerDropdown`) and the date-range selector (`DateRangeDropdown`) in `AgentEvalDetail.tsx` instead of building a new component — a `Button` (secondary, `icon`+`iconRight="ChevronDown"`) as the `trigger` matches the existing `RunEval`/`Compare` button styling for free. ref: `client/src/vendor/ui/kit/Dropdown.tsx`, `client/src/app/eval/_components/AgentEvalDetail.tsx`

- **2026-07-14** — T7 (Multi-Agent Review nav item): both the sidebar-highlight branch (`activeKeyFor('/multi-agent')`, `components/app-shell/helpers.ts:28`) and the i18n label (`shell.json:26`, `"multi-agent": "Multi-Agent Review"`) were pre-staged before this task — same pattern T13 hit for `/eval`. Only the `NavItemDef` in `vendor/ui/nav.ts` was missing. Added it to the WORKSPACE section (icon `Users`, gKey `m`, href `/multi-agent`, no `:repoId` token since the feature is workspace-scoped not per-repo) plus a matching `g m` row in `SHORTCUTS` — every other WORKSPACE/SKILLS-LAB item has one, so a shortcut-list entry is part of "match the shape of sibling entries," not scope creep. ref: `client/src/vendor/ui/nav.ts:46-52`, `client/src/components/app-shell/helpers.ts:28`, `client/messages/en/shell.json:26`

- **2026-07-14** — T8 (multi-agent client hooks): a task-mandated `*.test.ts` filename (not `.tsx`) for a hook test that needs `<QueryClientProvider>` as a `renderHook` wrapper still works — vitest's esbuild loader for `.ts` files does not parse JSX, so build the wrapper element with `React.createElement(QueryClientProvider, { client: qc }, children)` instead of JSX. Also: `renderHook(fn, { initialProps: { ids: [] } })` infers the props type from the literal `[]` as `never[]`, so a later `rerender({ ids: ["x"] })` fails typecheck (`string` not assignable to `never`) even though the callback itself is typed `{ ids: string[] }` — fix with `[] as string[]` in `initialProps`, not by loosening the callback's param type. ref: `client/src/lib/hooks/multiAgent.test.ts`

- **2026-07-14** — T8: `vi.useFakeTimers()` + RTL's `waitFor` deadlocks (hits the 5s test timeout) when a debounced hook's `setTimeout` needs fake-timer advancement to resolve a TanStack Query fetch — `waitFor`'s own internal polling also runs through the faked clock, so `await waitFor(...)` after `vi.advanceTimersByTimeAsync(...)` never observes the resolved state reliably. For a hook-level debounce test (e.g. `useMultiAgentEstimate`'s 300ms debounce before firing `api.get`), skip fake timers entirely: assert the fetch has NOT fired synchronously after a `rerender`, then `await waitFor(() => expect(...), { timeout: 2000 })` on real timers — simpler and it passed first try. ref: `client/src/lib/hooks/multiAgent.test.ts`

- **2026-07-13** — T18: the client-side date-range filter (7/30/90/all days) on `AgentEvalDetail`'s runs table + trend chart is intentionally NOT memoized — `Date.now()` is read once per render as a plain `const nowMs`, then `runs.filter(...)` runs directly (no `useMemo`), since the run-history list is small (report-cap of the eval set, never hundreds of rows) and `now` would invalidate any memo every render anyway. The sub-header's run COUNT (`{runs} runs`) intentionally reads the UNFILTERED `history.length`, not the range-filtered list — only the runs table and trend chart narrow by range; the 3 metric tiles' `current`/`delta` values also stay server-computed/unfiltered (spec only asked to filter "the runs table AND the trend/metric window", not the headline metric cards). Changing the range also clears any in-progress Compare row selection (`setSelectedRunIds([])`) so a selected run can't silently disappear from view while still "selected". ref: `client/src/app/eval/_components/AgentEvalDetail.tsx`

- **2026-07-15** — T10 (Multi-Agent Columns): importing a component from a DIFFERENT top-level `app/` route segment via the `@/` alias (e.g. `client/src/app/multi-agent/[runId]/_components/MultiAgentColumns/MultiAgentColumns.tsx` importing `@/app/repos/[repoId]/pulls/[number]/_components/RunTraceDrawer`) works fine — Next.js's App Router only treats bracketed folder names specially for actual page/route resolution under `app/`; a plain module import of a component living inside another route's `_components/` folder resolves normally through the TS path alias (bracket characters are just literal path segments to the resolver). Reused `RunTraceDrawer` as-is this way instead of duplicating it, per the T10 spec. ref: `client/src/app/multi-agent/[runId]/_components/MultiAgentColumns/MultiAgentColumns.tsx`, `client/src/app/repos/[repoId]/pulls/[number]/_components/RunTraceDrawer/index.ts`

- **2026-07-15** — T10: `useRunEvents(runIds)` (`client/src/lib/hooks/reviews.ts:164`) returns one aggregate `{ events, running }` for ALL subscribed run ids combined (`RunEvent.runId` is the only per-run discriminator in the flat `events` array) — there is no per-run `running` flag. To derive one column's live status out of a shared multi-run subscription: (1) only pass `run_id`s whose PERSISTED `status === 'running'` into `useRunEvents` (done/failed columns don't need a live stream), then (2) per column, filter `events` by `event.runId === column.run_id` and look for a terminal `kind: 'error'` (→ failed, reason = `event.msg`) or `kind: 'result'` (→ done); absence of a terminal event just means "still showing the persisted status" — this covers both "genuinely still running" and "page reloaded, stream dropped, only have the last known persisted status" (spec-accepted per the T10 gotcha) with the same fallback branch. ref: `client/src/app/multi-agent/[runId]/_components/MultiAgentColumns/helpers.ts` (`resolveColumnStatus`), `client/src/lib/hooks/reviews.ts:164`

- **2026-07-14** — T13 (Configure-run page `/multi-agent`): there is no workspace-wide "list all PRs" endpoint (`server/docs/api-contracts.md` only has `GET /repos/:repoId/pulls`), so a PR selector at a repo-agnostic route needs a two-step repo→PR select (`useRepos()` then `usePulls(repoId)`), mirroring `pulls/[number]/page.tsx:36`'s existing `pulls?.find(p => p.number === Number(number))?.id` pattern to resolve a PR row's uuid. Built as URL search params (`?repo=&pr=`) rather than local state so the selection survives navigation/reload, per the task's own gotcha hint. ref: `client/src/app/multi-agent/_components/ConfigureRun.tsx`, `server/docs/api-contracts.md:21`

- **2026-07-14** — T13: `multiAgent.json`'s `configure` section has no label for the repo selector (only `prSelectorLabel: "Pull request"` exists) and this task's owned paths exclude the i18n file. Reused `agents.json`'s `context.repoLabel` ("Repository") via a second `useTranslations("agents")` call — same cross-namespace-reuse pattern T13's eval-dashboard predecessor used for `shell.json`'s `nav.agents`. Also reused `picker.runReview`/`picker.runReviewAriaLabel` (T9's `AgentPicker` strings) for the Configure page's own launch button, since the task brief explicitly quoted "Run multi-agent review (N)" as the required button text and `configure.launch` ("Launch review") doesn't match that wording — `configure.launching` ("Launching…") is used for the in-flight label instead. ref: `client/messages/en/agents.json` (`context.repoLabel`), `client/messages/en/multiAgent.json` (`picker.*`, `configure.*`)

- **2026-07-14** — T13: `useMultiAgentEstimate(prId, agentIds)` (T8) only returns estimate rows for the ids in its OWN `agentIds` arg — so a per-agent "no estimate yet" placeholder (AC-9, cold start) is best derived by checking whether `estimate.data?.agents.find(a => a.agent_id === agent.id)` is present at all, not by checking `isLoading`. This one check transparently covers three different "not ready" cases for free: the query hasn't fired yet (0 agents selected), the 300ms debounce window hasn't elapsed since the last toggle (stale `debouncedKey` → old response still cached), and the server genuinely has no history for that agent yet. ref: `client/src/app/multi-agent/_components/ConfigureRun.tsx`, `client/src/lib/hooks/multiAgent.ts:61`

- **2026-07-15** — T14 (Results page, `/multi-agent/[runId]`): `useMultiAgentRun` (T8) has no `refetchInterval`, so a running column's status never advances past its initial persisted snapshot on its own. `ResultsView` closes this gap itself rather than editing the T8 hook (out of owned paths): it subscribes `useRunEvents` only to columns whose PERSISTED `status === "running"`, and on the FIRST terminal event (`kind: "result" | "error"`) per `runId` (tracked in a `useRef<Set<string>>`, not re-invalidating on every subsequent SSE frame) calls `qc.invalidateQueries({queryKey:["multi-agent-run", runId]})` so the next render comes from the persisted read endpoint, never from the live stream's own payload. The `?view=` toggle is implemented as `router.replace` over `useSearchParams()` only — it never touches `useMultiAgentRun`'s query key, so switching modes provably cannot re-trigger a run (AC-23), and `AgentDisagreement` is mounted exactly once, outside the `mode === "columns" ? ... : ...` branch, so "identical in both modes" (AC-27) holds by construction rather than by duplicated JSX. ref: `client/src/app/multi-agent/[runId]/_components/ResultsView.tsx`, `client/src/lib/hooks/multiAgent.ts:41` (`useMultiAgentRun`)

- **2026-07-15** — Review-gate fix (AC-6/AC-7, `ConfigureRun`): the original T13 implementation gated `agentEstimateText` behind `selected` AND requested `useMultiAgentEstimate(prId, selectedAgentIds)` (selected ids only) — so an unchecked agent row rendered nothing, contrary to "every listed agent row shows an estimate or placeholder". Fix required TWO coordinated changes, not just dropping the `selected` check in the render helper: (1) call the hook with `listedAgentIds` (every agent in the list, from `useAgents()`), not `selectedAgentIds`, so every row has an entry to look up; (2) once the hook covers ALL listed agents, the server's own `summary` field (`estimate.data.summary`) now aggregates ALL of them, not the current checkbox selection — so the "≈N agents · parallel fan-out · est. X · est. Y" summary line can no longer read `estimate.data.summary` directly. Added `summarizeSelectedEstimate()` in `helpers.ts` that re-derives the fan-out summary for just the selected subset client-side, mirroring the server's `summariseEstimate` (`server/src/modules/multi-agent-runs/estimate.ts`) exactly: duration = MAX of known durations (parallel fan-out), cost = SUM of known costs, cold-start (null) agents excluded from both reductions but still counted in `agent_count`. ref: `client/src/app/multi-agent/_components/ConfigureRun.tsx`, `client/src/app/multi-agent/_components/helpers.ts` (`summarizeSelectedEstimate`), `server/src/modules/multi-agent-runs/estimate.ts`

- **2026-07-15** — T10 restyle (Columns mode → mockup): `themeForIndex(i).accent` from `agentTheme.ts` is a plain opaque hex string (e.g. `#ef4444`), not an rgba/CSS-var. To get an accent-TINTED icon-tile background without a hex→rgba conversion helper, append a 2-hex alpha suffix directly to the string in the inline style (`` `${accent}26` ``, i.e. `#ef444426` ≈ 15% opacity) — valid 8-digit hex-with-alpha CSS Color Level 4 syntax, supported in all evergreen browsers this project targets. Also: the mockup's "row of columns, 4-ish across, wrap or horizontal-scroll gracefully when narrow" was implemented as an outer `overflowX: "auto"` div wrapping an inner `display:flex; flexWrap:"wrap"` row (each card `flex: "0 0 280px"`) — wrapping handles most narrow cases for free, the scroll wrapper is just a fallback if a card's own min-width ever can't fit. ref: `client/src/app/multi-agent/[runId]/_components/MultiAgentColumns/AgentColumnCard.tsx`, `client/src/app/multi-agent/[runId]/_components/MultiAgentColumns/styles.ts`

- **2026-07-15** — MultiAgentTabs restyle: the shared `Tabs` primitive (`client/src/vendor/ui/kit/Tabs.tsx`) renders every tab's icon in one global colour (`var(--accent)`/`var(--text-muted)`) with no per-tab colour override, so it can't express "each agent's tab icon uses that agent's own theme accent" (design mock requirement). Built a plain custom `<button>` row directly in `MultiAgentTabs.tsx` instead of reusing `Tabs`, styled per-agent via `themeForIndex(i).accent`. Also confirmed `Tabs.tsx` itself never sets `role="tablist"`/`role="tab"` on its buttons (plain native `<button>`, relies on default button semantics for keyboard operability) — matched that same convention rather than adding ARIA tab-pattern roles, since an explicit `role="tab"` on a `<button>` overrides its implicit accessible role and silently breaks any test using `getByRole("button", ...)` to find it. ref: `client/src/app/multi-agent/[runId]/_components/MultiAgentTabs/MultiAgentTabs.tsx`, `client/src/vendor/ui/kit/Tabs.tsx`

- **2026-07-15** — MultiAgentTabs restyle: `ConfidenceNum` (`client/src/vendor/ui/primitives/ConfidenceNum.tsx`) already renders confidence as `"{pct}% conf"` internally (with a coloured dot) — a DIFFERENT code path from `finding.confShort` (`multiAgent.json`, also `"{pct}% conf"`) that the design mock explicitly asked for on the collapsed finding card's second line. The two produce visually near-identical text but are independent implementations — don't assume a test asserting `"95% conf"` is exercising `confShort` just because the string matches; check which component actually rendered it. ref: `client/src/vendor/ui/primitives/ConfidenceNum.tsx`, `client/messages/en/multiAgent.json` (`finding.confShort`)

- **2026-07-15** — MultiAgentTabs restyle: deliberately left the summary card's "View trace" button unwired (label only, no `onClick`) rather than mounting `RunTraceDrawer` — wiring it would mean replicating `MultiAgentColumns`' `traceRunId` state + SSE subscription pattern, a real feature addition beyond "restyle to match the mockup" and not in the task's kept-behaviors list (AC-24/25/26 only). Flagged as a follow-up instead of silently adding scope. ref: `client/src/app/multi-agent/[runId]/_components/MultiAgentTabs/AgentSummaryCard.tsx`

## Open Questions

- **2026-07-12** — T10 (shared eval components): `client/messages/en/eval.json` (landed by T3) is missing several keys the T10 brief/mockups need: `caseEditor.cancel`, `caseEditor.runOnSave`, `caseEditor.tabs.files`, `caseEditor.findingSkeleton`, `dashboard.compare.close`. Since `eval.json` is outside T10's owned paths, these components call `t()` on the expected key paths anyway (so they resolve automatically once added) but currently render next-intl's missing-key fallback for those specific strings. `dashboard.table.cost` was reused for the compare modal's cost tile label instead of inventing a new key. Whoever owns T12/T13 (or a follow-up i18n pass) should add the 5 missing keys. ref: `client/messages/en/eval.json`, `client/src/components/eval/EvalCaseEditorModal.tsx`, `client/src/components/eval/CompareRunsModal.tsx`

- **2026-07-12** — T10: `caseEditor.resultSummary` in `eval.json` is `"recall {recall}% · precision {precision}% · citation {citation}% · {duration}s"` — a metrics-based summary — while mock_6's last-run badge shows an "expected N finding(s), got M" phrasing instead. `EvalCaseEditorModal`'s `LastRunBadge` follows the shipped i18n key literally (recall/precision/citation/duration%), which cosmetically diverges from the mockup text but avoids inventing an untranslated string. ref: `client/messages/en/eval.json`, `client/src/components/eval/EvalCaseEditorModal.tsx:158`

- **2026-07-12** — T10: no single-case run endpoint exists in the T7/T8 server plan (only `POST /agents/:id/eval-runs` runs the whole set) and T9's `hooks/eval.ts` has no per-case run mutation either. `EvalCaseEditorModal`'s "Run case" button is therefore a pure `onRunCase?: () => void` callback prop — the modal stays presentational and the caller (T12/T13) decides how (or whether) to wire it once/if a per-case run path exists. Likewise `onSave` takes a full `EvalCaseInput` and lets the caller pick create vs. update. ref: `client/src/components/eval/EvalCaseEditorModal.tsx`, `docs/plans/eval-pipeline.md` (T7/T8/T9 sections)

- **2026-07-11** — T10 (Onboarding Tour): the rewritten `generate.title`/`generate.body` keys in `onboarding.json` ended up wired only as a `title` (native tooltip) attribute on the Regenerate button — there is no natural "not yet generated" empty state to use them in, since the `OnboardingTour` contract always returns either a full tour or a degraded skeleton (never "no tour yet"). `generate.cta`/`generate.generating` are currently unused. A future session touching this page should decide whether to give them a real UI slot or prune them. Investigated in: `client/messages/en/onboarding.json`, `client/src/app/repos/[repoId]/onboarding/_components/OnboardingTourView/_components/TourHeader.tsx`.

- **2026-07-10** — T11 note (not an open question, logged here for T13/T14 to consume): a repos-list hook already exists — `useRepos()` in `client/src/lib/hooks/repos.ts:8`, returning `Repo[]` from `GET /repos`, exported via the `lib/hooks` barrel. T13/T14's Context tab repo selector (per R-7: agents/skills are workspace-scoped, discovery is per-repo) should reuse this hook rather than creating a new one. ref: `client/src/lib/hooks/repos.ts:8`

- **2026-07-10** — New `messages/en/<ns>.json` namespace files do NOT need registering anywhere outside the file itself. `client/src/i18n/request.ts:16` (`loadMessages`) does `readdirSync(messages/en/)` and merges every `*.json` file found into `{ [ns]: {...} }` at request time — dropping a new namespace file in is sufficient, `useTranslations("<ns>")` picks it up automatically. Confirmed while adding `projectContext.json` for T12 — no other file needed editing. ref: `client/src/i18n/request.ts:16-25`

- **2026-07-10** — T12 (Project Context page) built on `@devdigest/ui`'s `Drawer` + `Tabs` + `Markdown` kit components for a Preview/Edit drawer pattern (no prior example of this combo in the codebase): `Drawer`'s `footer` prop only needs to be non-`undefined` when the active tab wants one (e.g. hide Save while on the Preview tab). Mounting the drawer with `key={selectedPath}` at the parent (not inside the drawer itself) resets ALL local state — edit buffer text *and* the `useSaveDocument` mutation's `isSuccess`/`isError` flags — for free on document switch, avoiding a manual `mutation.reset()` call or extra `useEffect`. ref: `client/src/app/repos/[repoId]/project-context/_components/ProjectContextView/ProjectContextView.tsx`, `_components/DocumentDrawer.tsx`

- **2026-07-10** — A relative-time string ("5 minutes ago") that must go through `useTranslations` (no hardcoded English) is done by having the helper return a `{ unit, count }` tuple (pure, no i18n dependency) and letting the component pick the translation key + pass `count` for next-intl's ICU `plural` syntax (`"{count, plural, one {# minute ago} other {# minutes ago}}"`). Keeps the formatter testable in isolation while every rendered word stays translatable. ref: `client/src/app/repos/[repoId]/project-context/_components/ProjectContextView/helpers.ts`, `client/messages/en/projectContext.json`

- **2026-07-10** — At the point T12 finished, `cd client && pnpm typecheck` and `pnpm test` both fail, but ONLY due to `src/app/agents/[id]/_components/AgentEditor/_components/ContextTab/` (T13's owned path, run concurrently) — a `string | undefined` not assignable to `string` TS2322 at `ContextTab.tsx:78`, and one failing assertion in `ContextTab.test.tsx:132` (`getByText("Insights")` matches multiple elements). T12's own files (`project-context/`) compile clean and its 3 new tests pass in isolation. Flagging rather than fixing — `agents/**` is out of T12's owned paths. ref: `client/src/app/agents/[id]/_components/AgentEditor/_components/ContextTab/ContextTab.tsx:78`

- **2026-07-10** — RESOLVED (repair pass after T13/T14 were killed mid-run by an auth error): the TS2322 at `ContextTab.tsx:78` noted above was already self-fixed by a prior partial run (non-null assertions on the `next[idx]` swap in `moveAttached`) — no longer present. Two failures remained and were both test-only, components were correct: (1) skill tab's `ContextTab.test.tsx:97` had `toggles[toggles.length - 1]` typed `HTMLElement | undefined` under `noUncheckedIndexedAccess` — fixed with `.at(-1)` + a thrown guard instead of `!`, keeping the assertion honest rather than silencing the type; (2) agent tab's `ContextTab.test.tsx:132` `getByText("Insights")` matched TWO elements once the Preview drawer was open — the drawer's own bucket `Badge` AND the still-mounted background `DocRow`'s bucket badge for the SAME doc (Drawer renders inline, not hidden/portalled, so the list stays in the DOM). NOT the folder-path label as originally suspected (folder text is the raw lowercase path segment, badge text is the capitalized i18n translation — different strings). Fix: `within(screen.getByRole("dialog")).getByText(...)` for all 4 metadata assertions. ref: `client/src/app/agents/[id]/_components/AgentEditor/_components/ContextTab/ContextTab.tsx:78`, `client/src/app/agents/[id]/_components/AgentEditor/_components/ContextTab/ContextTab.test.tsx:132`, `client/src/vendor/ui/kit/Drawer.tsx:26`

- **2026-07-11** — T10 (Onboarding Tour page): `MermaidDiagram({chart})` already validates/no-ops internally (`mermaid.parse` with `suppressErrors`, renders `null` on invalid/unparseable input) — so passing `OnboardingTour.sections.architecture.diagram` (`string | null`) straight through only needs an outer `{diagram && <MermaidDiagram chart={diagram} />}` guard for the `null` case; no client-side mermaid sanitization needed on top of what the component already does. ref: `client/src/components/mermaid-diagram/MermaidDiagram.tsx:22`, `client/src/vendor/shared/contracts/onboarding.ts:46`

- **2026-07-11** — T10: the shared `Markdown` primitive (`client/src/vendor/ui/primitives/Markdown.tsx`) has no `components`-override prop, so it cannot render inline code spans as clickable links. For "inline code refs must be clickable Open affordances" (AC-6) without touching the shared primitive, split the narrative on backtick spans locally and render each segment as a plain React text/code node, linking only segments that exactly match a server-grounded `codeRefs` path — everything else (including unverifiable paths) stays plain `<code>`, which also satisfies AC-13's "drop/de-link unverifiable paths" for free, with zero `dangerouslySetInnerHTML`. ref: `client/src/app/repos/[repoId]/onboarding/_components/OnboardingTourView/helpers.ts`, `client/src/vendor/ui/primitives/Markdown.tsx`

- **2026-07-11** — T10: `githubBlobUrl()` in `client/src/lib/utils/githubUrls.ts` requires a commit `sha` (built for the PR-detail use case, pins line links to a PR's head) — not usable for a repo-wide, non-PR-scoped feature. For the Onboarding Tour's "Open" links, built a local `githubOpenUrl(repoFullName, defaultBranch, path)` using `Repo.default_branch` from `useActiveRepo()` instead of modifying the shared util. ref: `client/src/app/repos/[repoId]/onboarding/_components/OnboardingTourView/helpers.ts`, `client/src/lib/utils/githubUrls.ts:24`

- **2026-07-11** — T10: `RepoNotFound`'s own CTA already navigates to `/onboarding` (the first-run wizard) as "the add-a-repo flow" — confirmed this is the correct target for a degraded/no-clone CTA that must NOT itself trigger indexing (a feature that should only guide the user to the existing add/refresh/index flow, not call `useResyncRepoIntel` directly, which would perform indexing from within the wrong feature). ref: `client/src/components/repo-not-found/RepoNotFound.tsx`, `client/src/lib/hooks/repo-intel.ts:38`

- **2026-07-12** — T9 (eval hooks): T8's route plan only defines `/agents/:id/eval-*` paths (case CRUD + run + history + compare + dashboard, plus `/eval-cases/:caseId` for PATCH/DELETE) — no skill-scoped eval route exists despite `EvalOwnerKind` including `"skill"` and the T9 brief asking for `useEvalCases(ownerKind, ownerId)`/`useEvalDashboard(ownerKind, ownerId)` signatures. Kept `ownerKind` as a parameter (used in the dashboard query key per the plan's literal key list, and for future-proofing) but always built the URL as `/agents/${ownerId}/...`. Also: `GET /agents/:id/eval-compare` requires the agent id in the path, but the plan's literal signature list says `useCompareRuns(baseId, headId)` with no agent id — implemented as `useCompareRuns(agentId, baseId, headId)` instead since the URL cannot be built without it; flagging in case T13 (the only consumer) assumed the 2-arg form. No dedicated "run all agents" endpoint exists either — `useRunAllAgents()` fans out one `POST /agents/:id/eval-runs` per agent id via `Promise.allSettled` so one slow/failing agent doesn't block the rest. ref: `client/src/lib/hooks/eval.ts`, `docs/plans/eval-pipeline.md:319`, `docs/plans/eval-pipeline.md:376`

- **2026-07-12** — T12 (Evals tab): confirms T9's finding — there is no single-case run endpoint, so the per-case row's "run" icon and the case editor's "Run case" button both call the SAME `useRunEvalSet(agentId)` (whole-set run) mutation; there is no way to run just one case. Per-case pass/fail/expected/got status is therefore only knowable for cases covered by the most recent in-session `POST /agents/:id/eval-runs` response (`EvalRun.per_trace[]`, matched to a case by `trace.name === case.name`) — kept in local `useState`, not persisted, not refetched. A case that was run in a previous browser session (or by another tab) always renders "never run" until this tab runs the set again. The one exception is the top "Eval cases (M/N passing)" badge, which uses the SERVER-PERSISTED `dashboard.current.traces_passed`/`traces_total` (from `useEvalDashboard`) instead of session state, so that number stays accurate across reloads even though individual row icons don't. ref: `client/src/app/agents/[id]/_components/AgentEditor/_components/EvalsTab/EvalsTab.tsx`

- **2026-07-12** — T12 (Evals tab): `client/messages/en/eval.json` has no key for "expected N finding(s), got M" (mock_5's per-case subtext), "Run all evals", "New eval case" as a full CTA, "View full dashboard", the traces-passed tile label, or the "M/N passing"/"empty []" badges — none of these exist under `evalsTab.*` today. Per the task's explicit constraint (T12 must NOT edit `eval.json` — a separate integration step owns net-new keys), `EvalsTab.tsx` calls `t()` on the expected key paths anyway (`evalsTab.expectedGot`, `evalsTab.runAll`, `evalsTab.viewDashboard`, `evalsTab.tracesPassed`, `evalsTab.passingCount`, `evalsTab.emptyBrackets`, `evalsTab.loadError`) — same fallback pattern T10 already used for its own missing keys — so they resolve automatically once the integration step adds them; until then next-intl renders the literal key path (confirmed non-crashing per `client/insights/gotchas.md`'s "missing keys render as the key string, not an error"). Reused `dashboard.metrics.*` (RECALL/PRECISION/CITATION ACCURACY) and `dashboard.underMin` verbatim since they already fit. ref: `client/messages/en/eval.json`, `client/src/app/agents/[id]/_components/AgentEditor/_components/EvalsTab/EvalsTab.tsx`

- **2026-07-12** — T12: at the point this task finished, `cd client && pnpm typecheck` fails, but ONLY in `src/app/eval/_components/AgentEvalDetail.tsx` and `src/app/eval/page.tsx` (T13's owned paths, run concurrently) — undefined `agents`/`onSelectAgent`/`Dropdown` names, an invalid `"GitCompare"` icon, and a missing `./_components/EvalDashboardView` module. None of T12's own files (`EvalsTab.tsx`, `constants.ts`, `page.tsx` under `agents/[id]/`, `AgentEditor.tsx`) appear in the error list — confirmed by grepping the typecheck output for those paths (zero matches). `cd client && pnpm test` has 3 pre-existing failures in `FindingsPanel.test.tsx` ("No QueryClient set") that are T11's known, already-documented gap (see the 2026-07-12 T11 entry above) — unrelated to T12, not caused or fixed by this session. `AgentEditor.test.tsx` (which now renders 4 tabs instead of 3) still passes unmodified. ref: `client/src/app/eval/_components/AgentEvalDetail.tsx:239`, `client/src/app/eval/page.tsx:1`

- **2026-07-13** — `EvalDashboardView`'s new "AGENTS" section-heading label and the recent-runs table's owner-column header both render the literal text "Agents" (the table header reuses `tShell("nav.agents")`, which is also "Agents" in `shell.json`) — a test asserting the new section heading rendered cannot use `getByText("Agents")` (throws "multiple elements"); use `getAllByText("Agents").length >= 2` instead. ref: `client/src/app/eval/_components/EvalDashboardView.tsx` (`SectionHeading`), `client/src/app/eval/_components/EvalDashboardView.test.tsx`

- **2026-07-13** — Fixed the all-agents Eval Dashboard showing a card for every reviewer agent (including 4 that had zero eval cases and a stale seeded `model`, since `AgentSummaryCard`'s model badge always reads the LIVE `agent.model` — the bug was never a stale badge, it was rendering agents that should be absent). Root cause: `EvalDashboard` had no per-owner eval-case count, so the client couldn't distinguish "never configured for eval" from "configured but never run". Added `EvalDashboard.owner_case_counts: Record<string, number>` (server-computed, `.default({})`) and filtered `agentList` down to `evaluableAgents = agentList.filter(a => (caseCounts[a.id] ?? 0) > 0)` before building summary cards AND before the "Run all agents" mutation's agent-id list — a zero-case agent now never renders and never gets swept into a bulk run. ref: `client/src/app/eval/_components/EvalDashboardView.tsx`, `server/src/modules/eval/repository.ts` (`ownerCaseCounts`)

- **2026-07-12** — T17: `FindingCard.test.tsx` (pre-existing, NOT touched by T17 — out of its owned paths) logs a `next-intl` `MISSING_MESSAGE: Could not resolve 'eval' in messages for locale 'en'` console error on every test run: `FindingCard.tsx` now calls `useTranslations("eval")` for the "Turn into eval case" button (landed by T11), but `FindingCard.test.tsx`'s own `renderWithIntl` helper only supplies `{ prReview: messages }`. The tests still pass (next-intl's missing-key fallback doesn't throw), but the console noise is real and that file's existing assertions never actually verify the button's real accessible name. A future pass touching `FindingCard.test.tsx` should add `eval: evalMessages` to its provider, mirroring the fix applied to `FindingsPanel.test.tsx` this session. Investigated in: `client/src/app/repos/[repoId]/pulls/[number]/_components/FindingCard/FindingCard.test.tsx`, `client/src/app/repos/[repoId]/pulls/[number]/_components/FindingCard/FindingCard.tsx:48`.

- **2026-07-15** — T10 (Multi-Agent Columns): `RunTraceDrawer`'s `findings?: FindingRecord[]` prop expects the FULL `FindingRecord` shape (`rationale`, `suggestion`, `confidence`, `accepted_at`, `dismissed_at`, etc.), but a multi-agent column's findings are the narrower `AgentColumnFinding` (`observability.ts`) — `{id, severity, category, title, file, start_line, kind}` only, no rationale/suggestion/confidence/triage timestamps. `MultiAgentColumns` therefore mounts `RunTraceDrawer` with `findings={[]}` (the Trace tab's own persisted-trace data still loads fine via `runId`; only the drawer's inline "Findings" section has nothing to show for a multi-agent column today). If a future task wants the drawer's Findings section populated from a multi-agent run, either widen `AgentColumnFinding` to match `FindingRecord` (contracts are out of T10's owned paths) or fetch the full `FindingRecord[]` for that run_id separately before opening the drawer. Investigated in: `client/src/app/multi-agent/[runId]/_components/MultiAgentColumns/MultiAgentColumns.tsx`, `client/src/vendor/shared/contracts/observability.ts:23-32`, `client/src/app/repos/[repoId]/pulls/[number]/_components/RunTraceDrawer/RunTraceDrawer.tsx:24-25`.
